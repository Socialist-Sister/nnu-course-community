# 协作约定

1. 从 `main` 创建个人功能分支，修改后提交 Pull Request，说明改动及验证结果。
2. 由项目负责人审核合并并负责上线，不直接修改生产数据库或服务器配置。
3. 保持现有界面风格与共用导航、下拉组件；更改账号、评价或课程数据结构前先沟通。
4. 课程补充须保留旧课程和评价关联，使用可重复执行的明确迁移，不能用本地库覆盖线上库。
5. 不提交 `.env`、SMTP 密码、SSH 私钥、初始化码、数据库、备份、包含账号或会话信息的日志及截图。不要在 Issue 或 PR 粘贴这些内容。
6. 测试使用独立数据库和测试邮箱；未经授权不向用户发信或操作线上账号。
7. 评价公开展示昵称，不公开登录用户名、校园邮箱或账号标识。账号注销须保留评价并解除账号关联。
8. 本仓库没有自动部署流程。`deploy` 内脚本面向既有生产环境，运行前必须由负责人确认适用配置，协作者不应直接执行。

## 管理员权限

首位管理员为唯一站点负责人，在后台“管理员管理”中按完整登录用户名查找已验证校园邮箱的账号，确认后授予内容审核、课程维护中的一项或两项权限。负责人可调整或撤销授权，普通管理员不能授权其他成员。

内容审核仅可处理评价与举报；课程维护仅可修订课程、教师与分类；管理员名单及完整操作记录由负责人查看。撤销权限后旧会话立即失去后台操作权限。负责人不能被降权或自行注销。

迁移会依据原初始化记录识别现有负责人；若旧库只有一个管理员则使用该账号。多个旧管理员且无初始化记录时会中止迁移，须由负责人核对，不能随意选取。
